/* hi there
*/
lol
the templates are:    
*a foo* likes *a bar* and *a baz*

the knowledge base includes:

scenario one is:
foo *foo*

query two is:
foo if bar then unknown foo it is the case that blah
unknown hi foo