/* hi there
*/
lol
the templates are:    
*foo* likes *bar* and *baz*

the knowledge base includes:

scenario one is:
foo *foo*

query two is:
foo if bar then unknown foo it is the case that blah
unknown hi foo